import React from 'react';

const Background = () => {
    return (
        <section id="background" className="color-block">
            <h2>Background</h2>
            <p className="background-text">
                Technical lead for global production rollouts (Spotify, Microsoft). Managed 400+ assets across 10+ languages with zero-error delivery standards. The discipline required to ship at that scale — version control, cross-platform QA, stakeholder coordination — is the same discipline I bring to investigative and advocacy work. The difference is the client.
            </p>
        </section>
    );
};

export default Background;
